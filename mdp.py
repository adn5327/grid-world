from space import *

actions = [(0, 1), (0, -1), (1, 0), (-1, 0)]

def action_utility(s, )

def utility(spacey, maze_obj):
	cur_y, cur_x = spacey[0], spacey[1]



def value_iteration(iterations = 50, maze_obj, terminal = True):

	for n in range(iterations):
		for i in range(maze_obj.size):
			for j in range(maze_obj.size):

		# Change utilities here
